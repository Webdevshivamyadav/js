let str = "shivamdv      sfd";

// a array converison in to a string with join()

let arr = ['shivam','yadav','webdev'].join('');

// for converting a string in to array ;

let newindex = str.split("");

//  for clealing a triming a space from right sides an left sides 

let trimstr = str.trim();

// for removig space from the all side left,right,mid

let removespace = str.replace(/\s+/g,"")

// for returing a charchter on the specfic spaaces 

let newchar = str.charAt('2');

// for returing a chrchter index with negitive and postive with at()

let at = str.at('-1') // retrurn a charter from end

